## 1. Implementación

- [x] 1.1 Agregar manejo de `→` y `←` en `on_key` de `MainScreen` (`src/mango/tui/app.py`)

## 2. Verificación manual

- [x] 2.1 Probar `→` desde `category-list` mueve el foco a `macro-list` sin cambiar el contenido
- [x] 2.2 Probar `←` desde `macro-list` mueve el foco a `category-list`
- [x] 2.3 Probar que `→`/`←` en `shortcut-input` mueven el cursor de texto normalmente
- [x] 2.4 Probar que `→` desde `macro-list` y `←` desde `category-list` no hacen nada

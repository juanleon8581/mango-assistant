## 1. Implementación

- [ ] 1.1 Agregar manejo de `→` y `←` en `on_key` de `MainScreen` (`src/mango/tui/app.py`)

## 2. Verificación manual

- [ ] 2.1 Probar `→` desde `category-list` mueve el foco a `macro-list` sin cambiar el contenido
- [ ] 2.2 Probar `←` desde `macro-list` mueve el foco a `category-list`
- [ ] 2.3 Probar que `→`/`←` en `shortcut-input` mueven el cursor de texto normalmente
- [ ] 2.4 Probar que `→` desde `macro-list` y `←` desde `category-list` no hacen nada
